for forntend running
python -m http.server 8080 --bind 0.0.0.0
or
python -m http.server 8080

for backend running
python main.py
or
uvicorn main:app --host 0.0.0.0 --port 8000

for tunnel
lt --port 8080
then copy the url given, get the password and into to access.


